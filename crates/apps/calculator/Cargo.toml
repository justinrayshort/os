[package]
name = "desktop_app_calculator"
version = "0.1.0"
edition = "2021"

[features]
default = ["csr"]
csr = ["leptos/csr"]

[dependencies]
leptos = { version = "0.6", default-features = false }
serde_json = "1"
