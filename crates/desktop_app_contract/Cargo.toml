[package]
name = "desktop_app_contract"
version = "0.1.0"
edition = "2021"

[features]
default = ["csr"]
csr = ["leptos/csr"]

[dependencies]
leptos = { version = "0.6", default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
